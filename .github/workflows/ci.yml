name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
      with:
        submodules: true
    - name: Install PF_RING repository
      run: |
        wget https://packages.ntop.org/apt-stable/20.04/all/apt-ntop-stable.deb
        sudo apt-get install -y ./apt-ntop-stable.deb
        sudo apt-get update
    - name: Install build dependencies
      run: sudo apt-get install -y build-essential cmake libboost-program-options-dev libpcap-dev libtins-dev pfring pkg-config zlib1g-dev
    - name: Build
      run: |
        mkdir build && cd build
        cmake -DUSE_PF_RING=ON .. && cmake --build .
    - name: Test
      run: build/diamond-miner-prober --help
